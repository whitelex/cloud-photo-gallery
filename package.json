{
  "name": "cloud-photo-gallery",
  "version": "1.0.0",
  "description": "A simple and elegant web application to browse your photos.",
  "private": true,
  "scripts": {
    "build": "shx rm -rf dist && shx mkdir -p dist && shx cp -r components services *.html *.tsx *.ts *.json wrangler.toml dist/",
    "deploy": "wrangler pages deploy dist",
    "dev": "wrangler pages dev ."
  },
  "devDependencies": {
    "shx": "^0.3.4",
    "wrangler": "^3.57.0"
  }
}
